# Network-Intrusion-Detection-using-Machine-Learning-on-CIC-IDS2017

This repository contains the full implementation for a multiclass network
intrusion detection system, built and evaluated on the CIC-IDS2017 dataset.

## Repository Structure

```
.
├── README.md
├── data/
│   ├── Tuesday-WorkingHours_pcap_ISCX.csv
│   ├── Wednesday-workingHours_pcap_ISCX.csv
│   ├── Thursday-WorkingHours-Morning-WebAttacks_pcap_ISCX.csv
│   ├── Thursday-WorkingHours-Afternoon-Infilteration_pcap_ISCX.csv
│   ├── Friday-WorkingHours-Morning_pcap_ISCX.csv
│   ├── Friday-WorkingHours-Afternoon-PortScan_pcap_ISCX.csv
│   └── Friday-WorkingHours-Afternoon-DDos_pcap_ISCX.csv
├── notebooks/
│   ├── data_preparation.ipynb
│   └── modelling_and_evaluation.ipynb
├── processed_data/
│   (generated by notebook 01, not committed, see below)
├── saved_model/
│   ├── final_xgboost_model.joblib
│   ├── scaler.joblib
│   ├── label_encoder.joblib
│   └── feature_names.joblib
├── results/
│   ├── confusion_matrix_test.png
│   └── test_set_results_per_class.csv
└── report/
    └── report.pdf
```

## Dataset

This project uses seven CSV files from the CIC-IDS2017 dataset
(MachineLearningCVE version), covering Tuesday, Wednesday, both Thursday
files, and all three Friday files. Monday's file was excluded as it
contains only benign traffic.

The dataset was originally published by the Canadian Institute for
Cybersecurity:

Sharafaldin, I., Lashkari, A.H. and Ghorbani, A.A. (2018) Toward generating
a new intrusion detection dataset and intrusion traffic characterization.
*Proceedings of the 4th International Conference on Information Systems
Security and Privacy (ICISSP 2018)*, pp. 108-116.

Full dataset available at: https://www.unb.ca/cic/datasets/ids-2017.html

## Setup Instructions

1. Clone this repository:
   ```
   git clone <repository-url>
   cd <repository-name>
   ```

2. Create a Python environment and install dependencies:
   ```
   pip install pandas numpy scikit-learn xgboost matplotlib seaborn joblib jupyter
   ```

3. Ensure the seven CSV files listed above are present in the `data/` folder.

4. Run `notebooks/01_data_preparation.ipynb` from top to bottom. This will:
   - Load and combine the seven CSV files
   - Perform exploratory data analysis
   - Clean the data
   - Split and scale the data
   - Save processed arrays to `processed_data/`

5. Run `notebooks/02_modelling_and_evaluation.ipynb` from top to bottom. This will:
   - Load the processed data from step 4
   - Train and compare a Random Forest model and an XGBoost model
   - Tune the XGBoost model via randomised hyperparameter search
   - Evaluate the final tuned model on the held-out test set
   - Save the final model, scaler, and label encoder to `saved_model/`

## Reproducing Results

The final model achieved the following performance on the held-out test set:

- Accuracy: 99.84%
- Macro F1: 0.914
- Weighted F1: 0.998

Full per-class results are available in `results/test_set_results_per_class.csv`
and the report.

## Notes

- `processed_data/` is not committed to this repository due to its size.
  It is fully regenerated by running notebook 01.
- The trained model and supporting objects in `saved_model/` are committed,
  since they are required to reproduce the demonstration in notebook 02
  without retraining.
